# 招聘工作台浏览器本地版

## 数据保存在哪里

业务数据保存在打开网页的当前浏览器网站存储中，不会写入 GitHub 仓库，也不会自动同步到其他浏览器或电脑。

同一个 GitHub Pages 地址，在不同电脑、不同浏览器或无痕窗口中打开时，各自看到独立的数据。

## 使用前必须知道

- 清理浏览器缓存或网站数据可能删除台账。
- 更换浏览器、电脑或 GitHub Pages 地址后，原数据不会自动迁移。
- 请定期进入“设置与迁移”，下载完整 JSON 备份。
- JSON 备份和 CSV 导出可能包含候选人联系方式与薪酬信息，请妥善保管。
- 此版本不需要验证码、公司统一登录、Cloudflare D1 或其他云数据库。

## GitHub Pages 发布目录

仓库中的 `docs/` 是可直接发布的网页目录。在 GitHub 仓库设置中，将 Pages 来源设为 `main` 分支的 `/docs` 目录。

修改源码后，在仓库根目录运行：

```bash
npm run build:github-pages
```

该命令会重新生成 `docs/`。提交并推送 `docs/` 后，GitHub Pages 会更新网页。
